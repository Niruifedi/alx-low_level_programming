#include "main.h"

/**
 * _isdigit - check for a digit (0 through 9)
 * @c: the int to be checked
 * Return: 1 if c is a digit, otherwise 0
 */
int _isdigit(int c)
{
	return ( c >= '0' && c <= '9');
}
