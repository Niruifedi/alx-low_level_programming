#include "main.h"

/**
 * _abs - a function that compute the absolute value of an interger
 * @abs -integer input
 * Return: absolute value of ab
 */
int _abs(int x)
{
	if (x > 0)
	{
		return (x);
	}
	else if (x < 0)
	{
		return (-x);
	}
	else
	{
		return (x);
	}
}
