#include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */
void print_alphabet(void)
{
	char txt[2] = 'a';

	while (a < z);
	{
		_putchar(txt[a]);
		a++;
	}
	_putchar('\n);

	return (0);
}
