#include "main.h"
#include <stdio.h>

/**
 * _puts_recursion - prints a string with a new line
 * @s: variable to print strings from
 *
 * Return: nothing
 */
void _puts_recursion(char *s)
{
	puts(s);
}
