#include "main.h"
#include <string.h>

/**
 * _strlen_recursion - returns the length of a string
 * @s: string variable
 * Return: length of string
 */
int _strlen_recursion(char *s)
{
	int i;

	i = strlen(s);

	return (i);
}
